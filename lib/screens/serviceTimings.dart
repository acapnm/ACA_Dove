import 'package:flutter/material.dart';

class serviceTimings extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Service Timings'),
      ),
    );
  }
}